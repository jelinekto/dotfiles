#!/bin/sh

[ -z "$llm_dir" ] && llm_dir='/var/lib/llm'

config="${XDG_CONFIG_HOME}/llama-swap/config.yaml"

cat "${config}.base" > "$config"

for llm in "${llm_dir}"/*.yaml; do
  printf '\n' >> "$config"
  cat "$llm" >> "$config"
done
