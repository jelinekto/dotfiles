# vim: set ft=yaml :

macros:
  "llama": >
    /usr/bin/llama-server --port ${PORT} --n-gpu-layers 99 --threads 8 --mlock --ctx-size 16384 --no-kv-offload
  "llm_dir": /var/lib/llm
  "ttl": 600

models:
